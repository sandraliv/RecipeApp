<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- When the button is selected, apply the gradient -->
    <item android:color="@android:color/transparent" android:state_selected="true"/>

    <!-- Default state: no tint (no background color change) -->
    <item android:color="@color/rosybrown_50"/>
</selector>

