<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Outer Background (normal color or transparent) -->
    <item android:drawable="@android:color/transparent" />


    <!-- Inner Shadow Effect (gradient with color stops) -->
    <item android:drawable="@drawable/active_button" />
</layer-list>
